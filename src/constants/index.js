import theme from './theme';
import images from '../asset';
import {
  scale,
  verticalScale,
  moderatedScale,
  convertName,
  getPlatformURI,
} from './responsive';
import * as tile from './tile';
export {
  theme,
  images,
  scale,
  verticalScale,
  moderatedScale,
  convertName,
  getPlatformURI,
  tile,
};
