import theme from './theme';
import images from '../asset';
import {scale, moderatedScale, convertName, getPlatformURI} from './responsive';
import * as tile from './tile';
export {
  theme,
  images,
  scale,
  moderatedScale,
  convertName,
  getPlatformURI,
  tile,
};
